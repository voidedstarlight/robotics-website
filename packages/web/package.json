{
	"devDependencies": {
		"chokidar-cli": "^3.0.0"
	},
	"name": "@grizzlyrobotics.org/web",
	"license": "MIT",
	"scripts": {
		"build": "rm -rf ../../dist/web; mkdir -p ../../dist/web; cp -r src ../../dist/web",
		"dev": "npm run build; chokidar \"**/*\" -c \"npm run build\" -p"
	},
	"type": "module"
}
